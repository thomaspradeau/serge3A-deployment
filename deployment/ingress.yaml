apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: serge3a-ingress
  namespace: user-tpradeau
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/websocket-services: "serge3a-service"
spec:
  rules:
  - host: serge3a.user.lab.sspcloud.fr # À vérifier selon le format exact du cours
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: serge3a-service
            port:
              number: 80